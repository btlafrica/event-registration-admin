export const returnVerificationStatus = (optionTitle, state) => {
  let status = false;
  switch (optionTitle) {
    case "Phone Number":
      status = state;

      break;
    case "Phone Number":
      status = state;

      break;
    case "Phone Number":
      status = state;

      break;

    default:
      break;
  }
};
